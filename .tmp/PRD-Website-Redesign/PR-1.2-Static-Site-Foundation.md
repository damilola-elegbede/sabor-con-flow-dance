# PR 1.2: Static Site Foundation

## PR Metadata

**Title**: Build Core Static Site Structure and Components  
**Description**: Implement HTML5 semantic structure, modular CSS architecture, and vanilla JavaScript foundation for the dance studio website  
**Dependencies**: PR 1.1 (Project Setup & Build System)  
**Estimated Effort**: 6-8 hours  
**Priority**: Critical Path  

## Implementation Overview

This PR creates the foundational HTML, CSS, and JavaScript structure for the Sabor con Flow Dance website, establishing a modern, accessible, and performant static site foundation with modular component architecture.

## File Structure Changes

```
src/
├── html/                       # NEW - HTML templates
│   ├── index.html             # Homepage template
│   ├── partials/              # Reusable HTML components
│   │   ├── header.html        # Navigation and branding
│   │   ├── footer.html        # Footer with social links
│   │   ├── hero.html          # Hero section component
│   │   └── contact-form.html  # Contact form component
│   └── pages/                 # Individual page templates
│       ├── classes.html       # Class listings
│       ├── instructors.html   # Instructor profiles
│       ├── pricing.html       # Pricing information
│       └── contact.html       # Contact page
├── css/                       # NEW - SCSS/CSS source files
│   ├── main.scss             # Main stylesheet entry point
│   ├── base/                 # Base styles and resets
│   │   ├── _reset.scss       # CSS reset
│   │   ├── _typography.scss  # Font and text styles
│   │   ├── _variables.scss   # CSS custom properties
│   │   └── _mixins.scss      # SCSS mixins
│   ├── components/           # Component-specific styles
│   │   ├── _header.scss      # Header/navigation styles
│   │   ├── _hero.scss        # Hero section styles
│   │   ├── _buttons.scss     # Button styles
│   │   ├── _forms.scss       # Form styles
│   │   ├── _cards.scss       # Card component styles
│   │   └── _footer.scss      # Footer styles
│   ├── layout/               # Layout and grid systems
│   │   ├── _grid.scss        # CSS Grid layouts
│   │   ├── _containers.scss  # Container styles
│   │   └── _spacing.scss     # Margin/padding utilities
│   └── pages/                # Page-specific styles
│       ├── _home.scss        # Homepage styles
│       ├── _classes.scss     # Classes page styles
│       └── _contact.scss     # Contact page styles
├── js/                       # NEW - JavaScript modules
│   ├── main.js              # Main entry point
│   ├── components/          # Component JavaScript
│   │   ├── navigation.js    # Mobile navigation
│   │   ├── hero.js          # Hero interactions
│   │   ├── forms.js         # Form handling
│   │   └── lazy-loading.js  # Image lazy loading
│   ├── utils/               # Utility functions
│   │   ├── dom.js           # DOM manipulation helpers
│   │   ├── events.js        # Event handling utilities
│   │   └── performance.js   # Performance monitoring
│   └── lib/                 # Third-party integrations
│       ├── analytics.js     # Vercel Analytics integration
│       └── speed-insights.js # Vercel Speed Insights
└── assets/                  # Static assets
    ├── images/              # Image assets
    ├── fonts/               # Custom fonts
    └── icons/               # SVG icons
```

## Implementation Steps

### 1. HTML5 Semantic Structure

```html
<!-- src/html/index.html -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Professional salsa and bachata dance lessons in [City]. Join Sabor con Flow Dance Studio for beginner to advanced classes.">
    <title>Sabor con Flow Dance Studio - Professional Salsa & Bachata Lessons</title>
    
    <!-- Preload critical resources -->
    <link rel="preload" href="/dist/css/main.css" as="style">
    <link rel="preload" href="/dist/fonts/primary-font.woff2" as="font" type="font/woff2" crossorigin>
    
    <!-- Critical CSS inline (generated by build) -->
    <style id="critical-css">/* Critical CSS will be inlined here */</style>
    
    <!-- Non-critical CSS -->
    <link rel="stylesheet" href="/dist/css/main.css" media="print" onload="this.media='all'">
    
    <!-- Favicon and manifest -->
    <link rel="icon" type="image/svg+xml" href="/dist/images/favicon.svg">
    <link rel="manifest" href="/dist/manifest.json">
    
    <!-- Open Graph and social media -->
    <meta property="og:title" content="Sabor con Flow Dance Studio">
    <meta property="og:description" content="Professional salsa and bachata dance lessons">
    <meta property="og:image" content="/dist/images/og-image.jpg">
    <meta property="og:type" content="website">
    
    <!-- Structured data for local business -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "DanceGroup",
      "name": "Sabor con Flow Dance Studio",
      "description": "Professional salsa and bachata dance lessons",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "123 Dance Street",
        "addressLocality": "City",
        "addressRegion": "State",
        "postalCode": "12345"
      },
      "telephone": "+1-555-0123",
      "email": "info@saborconflow.com"
    }
    </script>
</head>
<body>
    <!-- Skip link for accessibility -->
    <a href="#main-content" class="skip-link">Skip to main content</a>
    
    <!-- Header with navigation -->
    <header class="site-header" role="banner">
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <img src="/dist/images/logo.svg" alt="Sabor con Flow Dance Studio" width="150" height="60">
                </div>
                
                <nav class="main-navigation" role="navigation" aria-label="Main navigation">
                    <button class="nav-toggle" aria-expanded="false" aria-controls="nav-menu">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="hamburger"></span>
                    </button>
                    
                    <ul class="nav-menu" id="nav-menu">
                        <li><a href="/" aria-current="page">Home</a></li>
                        <li><a href="/classes">Classes</a></li>
                        <li><a href="/instructors">Instructors</a></li>
                        <li><a href="/pricing">Pricing</a></li>
                        <li><a href="/contact">Contact</a></li>
                        <li><a href="/book" class="cta-button">Book Now</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>
    
    <!-- Main content -->
    <main id="main-content" role="main">
        <!-- Hero section -->
        <section class="hero" aria-labelledby="hero-title">
            <div class="container">
                <div class="hero-content">
                    <h1 id="hero-title" class="hero-title">
                        Learn to Dance with 
                        <span class="highlight">Passion & Flow</span>
                    </h1>
                    <p class="hero-description">
                        Join our professional salsa and bachata classes in a welcoming environment. 
                        From beginners to advanced dancers, we'll help you find your rhythm.
                    </p>
                    <div class="hero-actions">
                        <a href="/classes" class="btn btn-primary">View Classes</a>
                        <a href="/contact" class="btn btn-secondary">Free Trial</a>
                    </div>
                </div>
                <div class="hero-media">
                    <img src="/dist/images/hero-dance.jpg" 
                         alt="Couple dancing salsa at Sabor con Flow studio"
                         loading="eager"
                         width="600" 
                         height="400">
                </div>
            </div>
        </section>
        
        <!-- Features section -->
        <section class="features" aria-labelledby="features-title">
            <div class="container">
                <h2 id="features-title" class="section-title">Why Choose Sabor con Flow?</h2>
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <svg aria-hidden="true" width="48" height="48"><!-- Icon --></svg>
                        </div>
                        <h3>Expert Instructors</h3>
                        <p>Learn from certified professionals with years of international experience.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">
                            <svg aria-hidden="true" width="48" height="48"><!-- Icon --></svg>
                        </div>
                        <h3>All Skill Levels</h3>
                        <p>Classes designed for beginners through advanced dancers.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon">
                            <svg aria-hidden="true" width="48" height="48"><!-- Icon --></svg>
                        </div>
                        <h3>Flexible Scheduling</h3>
                        <p>Multiple class times throughout the week to fit your schedule.</p>
                    </div>
                </div>
            </div>
        </section>
    </main>
    
    <!-- Footer -->
    <footer class="site-footer" role="contentinfo">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>Contact Info</h3>
                    <address>
                        123 Dance Street<br>
                        City, State 12345<br>
                        <a href="tel:+15550123">+1 (555) 0123</a><br>
                        <a href="mailto:info@saborconflow.com">info@saborconflow.com</a>
                    </address>
                </div>
                
                <div class="footer-section">
                    <h3>Quick Links</h3>
                    <ul class="footer-links">
                        <li><a href="/classes">Classes</a></li>
                        <li><a href="/pricing">Pricing</a></li>
                        <li><a href="/instructors">Instructors</a></li>
                        <li><a href="/contact">Contact</a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h3>Follow Us</h3>
                    <div class="social-links">
                        <a href="#" aria-label="Facebook">
                            <svg width="24" height="24" aria-hidden="true"><!-- Facebook icon --></svg>
                        </a>
                        <a href="#" aria-label="Instagram">
                            <svg width="24" height="24" aria-hidden="true"><!-- Instagram icon --></svg>
                        </a>
                        <a href="#" aria-label="YouTube">
                            <svg width="24" height="24" aria-hidden="true"><!-- YouTube icon --></svg>
                        </a>
                    </div>
                </div>
            </div>
            
            <div class="footer-bottom">
                <p>&copy; 2024 Sabor con Flow Dance Studio. All rights reserved.</p>
            </div>
        </div>
    </footer>
    
    <!-- JavaScript -->
    <script src="/dist/js/main.js" defer></script>
    
    <!-- Service Worker Registration -->
    <script>
        if ('serviceWorker' in navigator) {
            navigator.serviceWorker.register('/sw.js');
        }
    </script>
</body>
</html>
```

### 2. SCSS Architecture and Variables

```scss
// src/css/base/_variables.scss
:root {
  // Brand Colors
  --color-primary: #e74c3c;        // Passionate red
  --color-primary-light: #f39c12;  // Warm orange
  --color-primary-dark: #c0392b;   // Deep red
  --color-secondary: #2c3e50;      // Elegant navy
  --color-accent: #f1c40f;         // Golden yellow
  
  // Neutral Colors
  --color-white: #ffffff;
  --color-black: #000000;
  --color-gray-100: #f8f9fa;
  --color-gray-200: #e9ecef;
  --color-gray-300: #dee2e6;
  --color-gray-400: #ced4da;
  --color-gray-500: #6c757d;
  --color-gray-600: #495057;
  --color-gray-700: #343a40;
  --color-gray-800: #212529;
  
  // Typography
  --font-primary: 'Inter', system-ui, sans-serif;
  --font-secondary: 'Playfair Display', serif;
  --font-mono: 'SF Mono', Monaco, monospace;
  
  // Font Sizes (fluid typography)
  --text-xs: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem);
  --text-sm: clamp(0.875rem, 0.8rem + 0.375vw, 1rem);
  --text-base: clamp(1rem, 0.9rem + 0.5vw, 1.125rem);
  --text-lg: clamp(1.125rem, 1rem + 0.625vw, 1.25rem);
  --text-xl: clamp(1.25rem, 1.1rem + 0.75vw, 1.5rem);
  --text-2xl: clamp(1.5rem, 1.3rem + 1vw, 1.875rem);
  --text-3xl: clamp(1.875rem, 1.6rem + 1.375vw, 2.25rem);
  --text-4xl: clamp(2.25rem, 1.9rem + 1.75vw, 3rem);
  
  // Spacing
  --space-xs: 0.5rem;
  --space-sm: 0.75rem;
  --space-md: 1rem;
  --space-lg: 1.5rem;
  --space-xl: 2rem;
  --space-2xl: 3rem;
  --space-3xl: 4rem;
  
  // Layout
  --container-max-width: 1200px;
  --container-padding: var(--space-md);
  --section-padding: var(--space-3xl);
  
  // Borders
  --border-radius: 0.375rem;
  --border-radius-lg: 0.5rem;
  --border-width: 1px;
  
  // Shadows
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
  --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
  
  // Transitions
  --transition-fast: 150ms ease;
  --transition-base: 250ms ease;
  --transition-slow: 350ms ease;
  
  // Z-index
  --z-dropdown: 1000;
  --z-sticky: 1020;
  --z-fixed: 1030;
  --z-modal: 1040;
  --z-popover: 1050;
  --z-tooltip: 1060;
  
  // Breakpoints (for JavaScript)
  --breakpoint-sm: 640px;
  --breakpoint-md: 768px;
  --breakpoint-lg: 1024px;
  --breakpoint-xl: 1280px;
}

// SCSS Variables for mixins
$breakpoints: (
  'sm': 640px,
  'md': 768px,
  'lg': 1024px,
  'xl': 1280px,
  'xxl': 1536px
);
```

```scss
// src/css/base/_mixins.scss
// Responsive breakpoints
@mixin respond-to($breakpoint) {
  @if map-has-key($breakpoints, $breakpoint) {
    @media (min-width: map-get($breakpoints, $breakpoint)) {
      @content;
    }
  } @else {
    @warn "Unknown breakpoint: #{$breakpoint}";
  }
}

// Container mixin
@mixin container {
  width: 100%;
  max-width: var(--container-max-width);
  margin-left: auto;
  margin-right: auto;
  padding-left: var(--container-padding);
  padding-right: var(--container-padding);
}

// Button base styles
@mixin button-base {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-sm) var(--space-lg);
  border: var(--border-width) solid transparent;
  border-radius: var(--border-radius);
  font-weight: 600;
  text-decoration: none;
  cursor: pointer;
  transition: all var(--transition-base);
  
  &:focus {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }
  
  &:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
}

// Visually hidden (screen reader only)
@mixin sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

// Focus styles
@mixin focus-visible {
  &:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }
}
```

### 3. Component Styles

```scss
// src/css/components/_header.scss
.site-header {
  position: sticky;
  top: 0;
  z-index: var(--z-sticky);
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-bottom: var(--border-width) solid var(--color-gray-200);
  
  .header-content {
    @include container;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: var(--space-md);
    padding-bottom: var(--space-md);
  }
  
  .logo img {
    height: 40px;
    width: auto;
  }
}

.main-navigation {
  .nav-toggle {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    padding: var(--space-xs);
    
    @media (max-width: 767px) {
      display: block;
    }
  }
  
  .nav-menu {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
    list-style: none;
    margin: 0;
    padding: 0;
    
    @media (max-width: 767px) {
      position: fixed;
      top: 100%;
      left: 0;
      right: 0;
      background: var(--color-white);
      flex-direction: column;
      padding: var(--space-lg);
      box-shadow: var(--shadow-lg);
      transform: translateY(-100%);
      transition: transform var(--transition-base);
      
      &.is-open {
        transform: translateY(0);
      }
    }
    
    a {
      color: var(--color-gray-700);
      text-decoration: none;
      font-weight: 500;
      transition: color var(--transition-fast);
      
      &:hover,
      &[aria-current="page"] {
        color: var(--color-primary);
      }
      
      @include focus-visible;
    }
    
    .cta-button {
      @include button-base;
      background: var(--color-primary);
      color: var(--color-white);
      
      &:hover {
        background: var(--color-primary-dark);
      }
    }
  }
}

.skip-link {
  @include sr-only;
  
  &:focus {
    position: fixed;
    top: var(--space-md);
    left: var(--space-md);
    z-index: var(--z-modal);
    background: var(--color-primary);
    color: var(--color-white);
    padding: var(--space-sm) var(--space-md);
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
  }
}
```

### 4. JavaScript Components

```javascript
// src/js/main.js
import { initNavigation } from './components/navigation.js';
import { initHero } from './components/hero.js';
import { initLazyLoading } from './components/lazy-loading.js';
import { initPerformanceMonitoring } from './utils/performance.js';
import { initAnalytics } from './lib/analytics.js';

// Initialize application
document.addEventListener('DOMContentLoaded', () => {
  // Core functionality
  initNavigation();
  initHero();
  initLazyLoading();
  
  // Performance and analytics
  initPerformanceMonitoring();
  initAnalytics();
  
  console.log('Sabor con Flow Dance Studio website initialized');
});

// Handle service worker updates
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.addEventListener('controllerchange', () => {
    window.location.reload();
  });
}
```

```javascript
// src/js/components/navigation.js
export function initNavigation() {
  const navToggle = document.querySelector('.nav-toggle');
  const navMenu = document.querySelector('.nav-menu');
  
  if (!navToggle || !navMenu) return;
  
  // Toggle mobile navigation
  navToggle.addEventListener('click', () => {
    const isOpen = navToggle.getAttribute('aria-expanded') === 'true';
    
    navToggle.setAttribute('aria-expanded', !isOpen);
    navMenu.classList.toggle('is-open', !isOpen);
    
    // Trap focus when menu is open
    if (!isOpen) {
      trapFocus(navMenu);
    }
  });
  
  // Close menu on escape key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && navMenu.classList.contains('is-open')) {
      closeMenu();
    }
  });
  
  // Close menu when clicking outside
  document.addEventListener('click', (e) => {
    if (!navToggle.contains(e.target) && !navMenu.contains(e.target)) {
      closeMenu();
    }
  });
  
  function closeMenu() {
    navToggle.setAttribute('aria-expanded', 'false');
    navMenu.classList.remove('is-open');
    navToggle.focus();
  }
  
  function trapFocus(element) {
    const focusableElements = element.querySelectorAll(
      'a[href], button, [tabindex]:not([tabindex="-1"])'
    );
    const firstFocusable = focusableElements[0];
    const lastFocusable = focusableElements[focusableElements.length - 1];
    
    element.addEventListener('keydown', (e) => {
      if (e.key === 'Tab') {
        if (e.shiftKey) {
          if (document.activeElement === firstFocusable) {
            lastFocusable.focus();
            e.preventDefault();
          }
        } else {
          if (document.activeElement === lastFocusable) {
            firstFocusable.focus();
            e.preventDefault();
          }
        }
      }
    });
    
    firstFocusable?.focus();
  }
}
```

```javascript
// src/js/components/lazy-loading.js
export function initLazyLoading() {
  // Native lazy loading fallback for older browsers
  if ('loading' in HTMLImageElement.prototype) {
    const images = document.querySelectorAll('img[loading="lazy"]');
    images.forEach(img => {
      img.src = img.dataset.src || img.src;
    });
  } else {
    // Intersection Observer fallback
    const imageObserver = new IntersectionObserver((entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const img = entry.target;
          img.src = img.dataset.src || img.src;
          img.classList.remove('lazy');
          observer.unobserve(img);
        }
      });
    });
    
    const lazyImages = document.querySelectorAll('img.lazy');
    lazyImages.forEach(img => imageObserver.observe(img));
  }
}
```

### 5. Performance Utilities

```javascript
// src/js/utils/performance.js
export function initPerformanceMonitoring() {
  // Core Web Vitals monitoring
  if ('web-vitals' in window) {
    import('web-vitals').then(({ getCLS, getFID, getFCP, getLCP, getTTFB }) => {
      getCLS(sendToAnalytics);
      getFID(sendToAnalytics);
      getFCP(sendToAnalytics);
      getLCP(sendToAnalytics);
      getTTFB(sendToAnalytics);
    });
  }
  
  // Resource timing monitoring
  monitorResourceLoading();
  
  // Page visibility tracking
  trackPageVisibility();
}

function sendToAnalytics(metric) {
  // Send to Vercel Analytics or other monitoring service
  if (window.va) {
    window.va('track', 'WebVital', {
      name: metric.name,
      value: metric.value,
      id: metric.id
    });
  }
  
  console.log('Performance metric:', metric);
}

function monitorResourceLoading() {
  window.addEventListener('load', () => {
    const navigation = performance.getEntriesByType('navigation')[0];
    const resources = performance.getEntriesByType('resource');
    
    // Log slow resources
    resources.forEach(resource => {
      if (resource.duration > 1000) {
        console.warn('Slow resource:', resource.name, `${resource.duration}ms`);
      }
    });
    
    // Track page load metrics
    sendToAnalytics({
      name: 'page-load',
      value: navigation.loadEventEnd - navigation.fetchStart,
      id: 'page-load-' + Date.now()
    });
  });
}

function trackPageVisibility() {
  let visibilityStart = Date.now();
  
  document.addEventListener('visibilitychange', () => {
    if (document.hidden) {
      const visibleTime = Date.now() - visibilityStart;
      sendToAnalytics({
        name: 'page-visible-time',
        value: visibleTime,
        id: 'visibility-' + Date.now()
      });
    } else {
      visibilityStart = Date.now();
    }
  });
}
```

## Testing Requirements

### Component Tests

```javascript
// tests/components/navigation.test.js
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { initNavigation } from '../../src/js/components/navigation.js';

describe('Navigation Component', () => {
  let container;
  
  beforeEach(() => {
    container = document.createElement('div');
    container.innerHTML = `
      <header class="site-header">
        <nav class="main-navigation">
          <button class="nav-toggle" aria-expanded="false">Menu</button>
          <ul class="nav-menu">
            <li><a href="/">Home</a></li>
            <li><a href="/classes">Classes</a></li>
          </ul>
        </nav>
      </header>
    `;
    document.body.appendChild(container);
  });
  
  afterEach(() => {
    document.body.removeChild(container);
  });
  
  it('should toggle mobile menu', () => {
    initNavigation();
    
    const toggle = container.querySelector('.nav-toggle');
    const menu = container.querySelector('.nav-menu');
    
    toggle.click();
    
    expect(toggle.getAttribute('aria-expanded')).toBe('true');
    expect(menu.classList.contains('is-open')).toBe(true);
  });
  
  it('should close menu on escape key', () => {
    initNavigation();
    
    const toggle = container.querySelector('.nav-toggle');
    const menu = container.querySelector('.nav-menu');
    
    // Open menu first
    toggle.click();
    expect(menu.classList.contains('is-open')).toBe(true);
    
    // Close with escape
    const escapeEvent = new KeyboardEvent('keydown', { key: 'Escape' });
    document.dispatchEvent(escapeEvent);
    
    expect(toggle.getAttribute('aria-expanded')).toBe('false');
    expect(menu.classList.contains('is-open')).toBe(false);
  });
});
```

### Accessibility Tests

```javascript
// tests/accessibility/a11y.test.js
import { describe, it, expect } from 'vitest';

describe('Accessibility', () => {
  it('should have proper heading hierarchy', () => {
    document.body.innerHTML = `
      <h1>Main Title</h1>
      <h2>Section Title</h2>
      <h3>Subsection Title</h3>
    `;
    
    const headings = Array.from(document.querySelectorAll('h1, h2, h3, h4, h5, h6'));
    const levels = headings.map(h => parseInt(h.tagName.charAt(1)));
    
    // Check for proper hierarchy (no skipped levels)
    for (let i = 1; i < levels.length; i++) {
      const diff = levels[i] - levels[i - 1];
      expect(diff).toBeLessThanOrEqual(1);
    }
  });
  
  it('should have alt text for images', () => {
    document.body.innerHTML = `
      <img src="test.jpg" alt="Test image">
      <img src="decorative.jpg" alt="">
    `;
    
    const images = document.querySelectorAll('img');
    images.forEach(img => {
      expect(img.hasAttribute('alt')).toBe(true);
    });
  });
  
  it('should have proper form labels', () => {
    document.body.innerHTML = `
      <form>
        <label for="email">Email</label>
        <input type="email" id="email" name="email">
      </form>
    `;
    
    const inputs = document.querySelectorAll('input, select, textarea');
    inputs.forEach(input => {
      const label = document.querySelector(`label[for="${input.id}"]`);
      expect(label).toBeTruthy();
    });
  });
});
```

## Completion Checklist

### HTML Structure
- [ ] Semantic HTML5 elements (header, nav, main, section, footer)
- [ ] Proper heading hierarchy (h1 → h2 → h3)
- [ ] ARIA labels and roles for accessibility
- [ ] Meta tags for SEO and social sharing
- [ ] Structured data for local business
- [ ] Skip links for keyboard navigation

### CSS Architecture
- [ ] SCSS modular structure with partials
- [ ] CSS custom properties for theming
- [ ] Responsive design with mobile-first approach
- [ ] Component-based styling system
- [ ] Utility classes for spacing and layout
- [ ] Consistent design tokens

### JavaScript Foundation
- [ ] ES6 modules with proper imports/exports
- [ ] Event delegation and performance optimization
- [ ] Accessibility features (focus management, ARIA)
- [ ] Error handling and graceful degradation
- [ ] Performance monitoring utilities
- [ ] Progressive enhancement approach

### Performance Optimization
- [ ] Critical CSS inlining
- [ ] Lazy loading for images
- [ ] Resource preloading for critical assets
- [ ] Font loading optimization
- [ ] Minification and compression
- [ ] Bundle size monitoring

### Accessibility Compliance
- [ ] WCAG 2.1 AA compliance
- [ ] Keyboard navigation support
- [ ] Screen reader compatibility
- [ ] Color contrast ratios
- [ ] Focus indicators
- [ ] Alternative text for images

### Testing Coverage
- [ ] Component unit tests
- [ ] Accessibility automated tests
- [ ] Performance regression tests
- [ ] Cross-browser compatibility tests
- [ ] Mobile responsive tests

## Performance Targets

- **First Contentful Paint**: < 1.5s
- **Largest Contentful Paint**: < 2.5s
- **Cumulative Layout Shift**: < 0.1
- **First Input Delay**: < 100ms
- **Page Load Time**: < 3s on 3G
- **Lighthouse Score**: > 90 (all categories)

## Browser Support

- **Modern Browsers**: Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- **Mobile**: iOS Safari 14+, Chrome Mobile 90+
- **Progressive Enhancement**: Graceful degradation for older browsers
- **JavaScript Disabled**: Core content and navigation functional

## Next Steps

After PR approval and merge:
1. Content population and imagery
2. Advanced interaction patterns
3. Form validation and submission
4. Integration with backend APIs
5. Preparation for PR 1.3: Testing Infrastructure